import {GithubIcon} from "./githubicon";
import {TypeScriptIcon} from "./TypeScriptIcon";
import { VSCodeIcon } from "./VisualStudioCodeIcon";
// importe ici toutes les icônes une seule fois

const icons = {
  GithubIcon,
  TypeScriptIcon,
  VSCodeIcon
  // Ajoute ici tes autres icônes
};

export default icons;